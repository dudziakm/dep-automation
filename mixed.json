{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "description": "Repo with JS and JVM manifests at the same time. Both layers; the rules of the two ecosystems do not overlap, because the selectors are disjoint.",

  "extends": [
    "github>dudziakm/dep-automation:js",
    "github>dudziakm/dep-automation:jvm"
  ]
}
